(function () {
  // Static fallback for environments where PHP is not executed.
  // Update on deploy to invalidate cached JS/CSS assets.
  window.SITE_VERSION = '2026-05-26-team-detail-fixes';
})();
